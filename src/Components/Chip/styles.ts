import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  chip: {
    paddingHorizontal: 4,
  },
  chipText: {
    paddingVertical: 3,
    paddingHorizontal: 6,
    borderRadius: 3,
    fontSize: 12,
  },
});

export default styles;
